version: '3.8'

services:
  bot:
    build: .
    container_name: fifa_world_cup_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./assets/flags:/app/assets/flags
      - ./assets/posters:/app/assets/posters
